# Pin Gradio to a known stable version
torch
gradio

# PyTorch must be listed first as other libraries depend on it during installation

# Pin transformers and accelerate to fix compatibility bugs with Phi-3
transformers
accelerate

# A dependency for attention mechanisms, good to have
einops

# LangGraph itself
langgraph
